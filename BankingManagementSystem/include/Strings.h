#ifndef STRINGS_H
#define STRINGS_H


class Strings
{
    public:
        Strings();
        virtual ~Strings();
        bool compareStrings(char string1[],char string2[]);

    protected:

    private:
};

#endif // STRINGS_H
